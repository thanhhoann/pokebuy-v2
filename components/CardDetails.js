import {
  Modal,
  ModalOverlay,
  ModalContent,
  ModalHeader,
  ModalCloseButton,
  ModalFooter,
  ModalBody,
  useDisclosure,
  Button,
} from "@chakra-ui/react";
export default function CardDetails({ isOpen }) {
  const { onOpen, onClose } = useDisclosure();
  return <></>;
}
